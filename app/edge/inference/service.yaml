apiVersion: v1
kind: Service
metadata:
  name: inference
  namespace: fire-detection
  labels:
    app: inference
    tier: edge
spec:
  selector:
    app: inference
  ports:
    - port: 9090
      targetPort: 9090
      name: metrics
