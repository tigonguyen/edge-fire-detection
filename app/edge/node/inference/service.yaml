apiVersion: v1
kind: Service
metadata:
  name: edge-fire-inference
  namespace: default
  labels:
    app: inference
    tier: edge
spec:
  selector:
    app: inference
    location: node
  ports:
    - port: 9090
      targetPort: 9090
      name: metrics
